<UserControl x:Name="exDatePicker" x:Class="CashManager_MVVM.Features.Common.ExtendedDatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DatePicker Grid.Row="0" SelectedDate="{Binding SelectedValue, StringFormat='yyyy.MM.dd', ElementName=exDatePicker}" FirstDayOfWeek="Monday"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractYearCommand, ElementName=exDatePicker}"/>
            <Button Grid.Column="0" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractMonthCommand, ElementName=exDatePicker}" Margin="20,0,0,0"/>
            <Button Grid.Column="0" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractDayCommand, ElementName=exDatePicker}" Margin="40,0,0,0"/>
            
            <Button Grid.Column="1" Content="Placeholder"/>

            <StackPanel Grid.Column="2" Width="60" HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                <Button Width="18" Content="test polygon" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractYearCommand, ElementName=exDatePicker}"/>
                <Button Width="18" Content="test polygon" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractMonthCommand, ElementName=exDatePicker}" />
                <Button Width="18" Content="test polygon" Template="{DynamicResource LeftArrowButton}" Command="{Binding ViewModel.SubtractDayCommand, ElementName=exDatePicker}" HorizontalAlignment="Left" />
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>